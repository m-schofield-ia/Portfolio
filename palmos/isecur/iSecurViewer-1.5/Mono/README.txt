The example Build.mak/Makefile _used_ to work for Mono.  However, after the
upgrade to Microsoft .NET 2.0 the files are probably incompatible.

When Mono is upgraded for .NET 2.0, I will adjust the files.

